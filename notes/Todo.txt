> Rename network_destroy and network_socket_create functions to DSNet functions, and use them as well
> Make sure that this works on a very low FPS as well (in case of lagspikes) - see connect as new client while low FPS -> causes trouble